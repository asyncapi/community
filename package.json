{
  "name": "@asyncapi/community",
  "description": "Repository to access AsyncAPI community assets & configs",
  "scripts": {
    "lint:links": "docker run --init -it --rm -w /input -v $(pwd):/input lycheeverse/lychee ./docs",
    "test": "jest",
    "generate:board": "node scripts/tsc-board-member.js"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@octokit/rest": "^21.0.0",
    "jest": "^29.7.0"
  }
}
